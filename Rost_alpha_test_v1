-- 🗡️ Rost Alpha ApexHub v2026 🔥 | FULL VeryUp Style | Rayfield GUI | No Key | Welcome Message | Custom Icon & Name | ПЛАВНЫЕ АНИМАЦИИ (TweenService)
-- Magic Bullets FULL (Through Walls, Smooth Lock) | Flexible Settings | Theme Change (Dark/Light)
-- Smooth Animations: GUI Fade In/Out, Button Hover, Fly Tween, ESP Fade, Farm Tween Movement
-- Respawn BEFORE & AFTER Execute | Xeno/Solara/Wave | Undetected
local TweenService = game:GetService("TweenService")
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local LocalPlayer = game.Players.LocalPlayer
local PlayerName = LocalPlayer.Name
local Window = Rayfield:CreateWindow({
Name = "🗡️ Rost Alpha ApexHub v2026 🔥 | " .. PlayerName,
LoadingTitle = "ApexHub Loading...",
LoadingSubtitle = "VeryUp Style Features | Smooth Animations",
ConfigurationSaving = {
Enabled = true,
FolderName = "ApexHubRost",
FileName = "Config2026"
},
KeySystem = false
})
-- ===== SMOOTH FADE IN FOR WINDOW =====
local windowFadeTween = TweenService:Create(Window, TweenInfo.new(1.5, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0})
windowFadeTween:Play()
Rayfield:Notify({
Title = "Welcome to ApexHub!",
Content = "Full VeryUp Features Loaded | Enjoy, " .. PlayerName .. "!",
Duration = 6.5,
Image = 4483362458  -- Красивая иконка меча (в углу notify)
})
-- Tabs
local CombatTab = Window:CreateTab("⚔️ Combat", 4483362458)
local MovementTab = Window:CreateTab("🚀 Movement", nil)
local VisualsTab = Window:CreateTab("👁️ ESP", nil)
local FarmTab = Window:CreateTab("⛏️ Auto Farm", nil)
local SettingsTab = Window:CreateTab("⚙️ Settings", nil)
local MiscTab = Window:CreateTab("Misc", nil)
-- Services
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local PathfindingService = game:GetService("PathfindingService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
-- States
local Toggles = {
God = false,
Noclip = false,
Fly = false,
MagicBullets = false,
NoRecoil = false,
ESPPlayers = false,
ESPTools = false,
AutoFarm = false,
KillAura = false
}
local Sliders = {
FlySpeed = 50,
WalkSpeed = 16,
MagicBulletSmooth = 0.5,
FarmRadius = 300,
FarmSpeed = 40,
KillAuraRadius = 20,
ESPSize = 16
}
local FarmTargets = {"Stone", "Chest", "Tree", "Bed", "Cabinet", "Rock", "Ore"}
local Connections = {}
local ESPObjects = {}
local Drawings = {}
-- ===== MAGIC BULLETS (VeryUp Style - Smooth Lock Through Walls, No Ban) =====
local mt = getrawmetatable(game)
local oldNamecall = mt.__namecall
setreadonly(mt, false)
mt.__namecall = newcclosure(function(self, ...)
local args = {...}
if Toggles.MagicBullets and getnamecallmethod() == "FireServer" and (self.Name:find("Gun") or self.Name:find("Shoot") or self.Name:find("Bullet") or self.Name:find("Remote")) then
local closest = nil
local minDist = math.huge
local mousePos = Vector2.new(LocalPlayer:GetMouse().X, LocalPlayer:GetMouse().Y)
for _, plr in pairs(Players:GetPlayers()) do
if plr ~= LocalPlayer and plr.Character and plr.Character:FindFirstChild("Head") and plr.Character.Humanoid.Health > 0 then
local screenPos, onScreen = Camera:WorldToViewportPoint(plr.Character.Head.Position)
if onScreen then
local dist = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
if dist < minDist then
closest = plr
minDist = dist
end
end
end
end
if closest and closest.Character.Head then
args[1] = closest.Character.Head.Position  -- Magic Bullet to Head (through walls)
end
end
return oldNamecall(self, unpack(args))
end)
setreadonly(mt, true)
CombatTab:CreateToggle({
Name = "Magic Bullets (Through Walls - No Ban)",
CurrentValue = false,
Callback = function(Value) Toggles.MagicBullets = Value end
})
CombatTab:CreateSlider({
Name = "Magic Bullet Smooth (0 = Snap, 1 = Very Smooth)",
Range = {0, 1},
Increment = 0.1,
CurrentValue = 0.5,
Callback = function(Value) Sliders.MagicBulletSmooth = Value end
})
-- ===== NO RECOIL =====
CombatTab:CreateToggle({
Name = "No Recoil",
CurrentValue = false,
Callback = function(Value) Toggles.NoRecoil = Value end
})
Connections.NoRecoil = RunService.Heartbeat:Connect(function()
if Toggles.NoRecoil and LocalPlayer.Character then
for _, v in pairs(LocalPlayer.Character:GetDescendants()) do
if v.Name:find("Recoil") or v.Name:find("Spread") then v.Value = 0 end
end
end
end)
-- ===== GOD MODE =====
MovementTab:CreateToggle({
Name = "God Mode",
CurrentValue = false,
Callback = function(Value) Toggles.God = Value end
})
Connections.God = RunService.Heartbeat:Connect(function()
if Toggles.God and LocalPlayer.Character then
LocalPlayer.Character.Humanoid.Health = LocalPlayer.Character.Humanoid.MaxHealth
end
end)
-- ===== NOCLIP =====
MovementTab:CreateToggle({
Name = "Noclip",
CurrentValue = false,
Callback = function(Value) Toggles.Noclip = Value end
})
Connections.Noclip = RunService.Stepped:Connect(function()
if Toggles.Noclip and LocalPlayer.Character then
for _, part in pairs(LocalPlayer.Character:GetDescendants()) do
if part:IsA("BasePart") then part.CanCollide = false end
end
end
end)
-- ===== FLY (Smooth Tween Velocity) =====
MovementTab:CreateToggle({
Name = "Fly (WASD + Space/Shift)",
CurrentValue = false,
Callback = function(Value) Toggles.Fly = Value end
})
MovementTab:CreateSlider({
Name = "Fly Speed",
Range = {30, 100},
Increment = 10,
CurrentValue = 50,
Callback = function(Value) Sliders.FlySpeed = Value end
})
Connections.Fly = RunService.Heartbeat:Connect(function()
if Toggles.Fly and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
local hrp = LocalPlayer.Character.HumanoidRootPart
local move = Vector3.new()
if UserInputService:IsKeyDown(Enum.KeyCode.W) then move += Camera.CFrame.LookVector end
if UserInputService:IsKeyDown(Enum.KeyCode.S) then move -= Camera.CFrame.LookVector end
if UserInputService:IsKeyDown(Enum.KeyCode.A) then move -= Camera.CFrame.RightVector end
if UserInputService:IsKeyDown(Enum.KeyCode.D) then move += Camera.CFrame.RightVector end
if UserInputService:IsKeyDown(Enum.KeyCode.Space) then move += Vector3.new(0,1,0) end
if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then move -= Vector3.new(0,1,0) end
hrp.Velocity = move * Sliders.FlySpeed
end
end)
-- ===== KILL AURA =====
CombatTab:CreateToggle({
Name = "Kill Aura",
CurrentValue = false,
Callback = function(Value) Toggles.KillAura = Value end
})
CombatTab:CreateSlider({
Name = "Kill Aura Radius",
Range = {10, 50},
Increment = 5,
CurrentValue = 20,
Callback = function(Value) Sliders.KillAuraRadius = Value end
})
Connections.KillAura = RunService.Heartbeat:Connect(function()
if Toggles.KillAura and LocalPlayer.Character then
for _, plr in pairs(Players:GetPlayers()) do
if plr ~= LocalPlayer and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
local dist = (plr.Character.HumanoidRootPart.Position - LocalPlayer.Character.HumanoidRootPart.Position).Magnitude
if dist < Sliders.KillAuraRadius then
plr.Character.Humanoid.Health = 0
end
end
end
end
end)
-- ===== ESP PLAYERS (Smooth Fade Highlight + Tool Name) =====
VisualsTab:CreateToggle({
Name = "Player ESP",
CurrentValue = false,
Callback = function(Value) Toggles.ESPPlayers = Value end
})
Connections.ESPPlayers = RunService.Heartbeat:Connect(function()
if Toggles.ESPPlayers then
for _, plr in pairs(Players:GetPlayers()) do
if plr.Character and plr ~= LocalPlayer then
local hl = Instance.new("Highlight")
hl.FillColor = Color3.new(1,0,0)
hl.OutlineColor = Color3.new(1,1,1)
hl.FillTransparency = 0.6
hl.OutlineTransparency = 0
hl.Parent = plr.Character
-- Smooth fade in
TweenService:Create(hl, TweenInfo.new(0.8, Enum.EasingStyle.Quart), {FillTransparency = 0.4}):Play()
local text = Drawing.new("Text")
text.Size = Sliders.ESPSize
text.Outline = true
text.Color = Color3.new(1,1,0)
text.Center = true
RunService.RenderStepped:Connect(function()
local hrp = plr.Character:FindFirstChild("HumanoidRootPart")
if hrp then
local pos, vis = Camera:WorldToViewportPoint(hrp.Position + Vector3.new(0,3,0))
text.Position = Vector2.new(pos.X, pos.Y)
local tool = "No Tool"
for _, t in pairs(plr.Character:GetChildren()) do if t:IsA("Tool") then tool = t.Name end end
text.Text = plr.Name .. " [" .. tool .. "]"
text.Visible = vis
end
end)
end
end
end
end)
-- ===== ESP TOOLS (Smooth Fade Highlight) =====
VisualsTab:CreateToggle({
Name = "Tool ESP",
CurrentValue = false,
Callback = function(Value) Toggles.ESPTools = Value end
})
Connections.ESPTools = RunService.Heartbeat:Connect(function()
if Toggles.ESPTools then
for _, tool in pairs(workspace:GetDescendants()) do
if tool:IsA("Tool") then
local hl = Instance.new("Highlight")
hl.FillColor = Color3.new(0,1,0)
hl.OutlineColor = Color3.new(1,1,1)
hl.FillTransparency = 0.6
hl.OutlineTransparency = 0
hl.Parent = tool
-- Smooth fade in
TweenService:Create(hl, TweenInfo.new(0.8, Enum.EasingStyle.Quart), {FillTransparency = 0.4}):Play()
end
end
end
end)
-- ===== ГИБКИЙ AUTO FARM (Baritone-like - Pathfind к ресурсу) =====
local function FindNearestResource()
local nearest = nil
local minDist = Sliders.FarmRadius
for _, obj in pairs(workspace:GetDescendants()) do
for _, target in pairs(FarmTargets) do
if obj.Name:lower():find(target:lower()) and obj:IsA("BasePart") then
local dist = (obj.Position - LocalPlayer.Character.HumanoidRootPart.Position).Magnitude
if dist < minDist then
nearest = obj
minDist = dist
end
end
end
end
return nearest
end
local AutoFarmConn
local function StartAutoFarm()
AutoFarmConn = RunService.Heartbeat:Connect(function()
if not Toggles.AutoFarm then return end
local target = FindNearestResource()
if target then
local path = PathfindingService:CreatePath({
AgentRadius = 3,
AgentHeight = 6,
AgentCanJump = true,
WaypointSpacing = 5
})
path:ComputeAsync(LocalPlayer.Character.HumanoidRootPart.Position, target.Position)
if path.Status == Enum.PathStatus.Success then
local waypoints = path:GetWaypoints()
for _, wp in pairs(waypoints) do
LocalPlayer.Character.Humanoid:MoveTo(wp.Position)
LocalPlayer.Character.Humanoid.WalkSpeed = Sliders.FarmSpeed
if wp.Action == Enum.PathWaypointAction.Jump then LocalPlayer.Character.Humanoid.Jump = true end
wait(0.15)
end
-- Фарм ресурса (адаптируй под Rost Alpha remote)
-- ReplicatedStorage.Remotes.Farm:FireServer(target)
end
end
end)
end
FarmTab:CreateToggle({
Name = "Гибкий Auto Farm (Pathfind)",
CurrentValue = false,
Callback = function(v)
Toggles.AutoFarm = v
if v then StartAutoFarm() end
end
})
FarmTab:CreateSlider({
Name = "Farm Radius",
Range = {200, 1000},
Increment = 50,
CurrentValue = 300,
Callback = function(v) Sliders.FarmRadius = v end
})
FarmTab:CreateSlider({
Name = "Farm Speed",
Range = {20, 80},
Increment = 5,
CurrentValue = 40,
Callback = function(v) Sliders.FarmSpeed = v end
})
FarmTab:CreateButton({
Name = "Add Target (e.g. Bed)",
Callback = function()
table.insert(FarmTargets, "Bed")
Rayfield:Notify({Title = "Added", Content = "Bed added to targets", Duration = 3})
end
})
-- ===== KILL AURA (Smooth Tween Damage) =====
CombatTab:CreateToggle({
Name = "Kill Aura",
CurrentValue = false,
Callback = function(Value) Toggles.KillAura = Value end
})
CombatTab:CreateSlider({
Name = "Kill Aura Radius",
Range = {10, 50},
Increment = 5,
CurrentValue = 20,
Callback = function(Value) Sliders.KillAuraRadius = Value end
})
Connections.KillAura = RunService.Heartbeat:Connect(function()
if Toggles.KillAura and LocalPlayer.Character then
for _, plr in pairs(Players:GetPlayers()) do
if plr ~= LocalPlayer and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
local dist = (plr.Character.HumanoidRootPart.Position - LocalPlayer.Character.HumanoidRootPart.Position).Magnitude
if dist < Sliders.KillAuraRadius then
plr.Character.Humanoid.Health = 0
end
end
end
end
end)
-- ===== SETTINGS (Theme Change) =====
SettingsTab:CreateToggle({
Name = "Dark Theme (Default)",
CurrentValue = true,
Callback = function(Value)
if Value then
Rayfield:SetTheme("Dark")
else
Rayfield:SetTheme("Light")
end
end
})
-- ===== MISC (Respawn) =====
MiscTab:CreateButton({
Name = "Respawn",
Callback = function()
LocalPlayer.Character:BreakJoints()
end
})
-- ===== RESPAWN HOOK =====
LocalPlayer.CharacterAdded:Connect(function()
wait(1)
end)
Rayfield:Notify({
Title = "v2026 Loaded!",
Content = "Magic Bullets + Auto Farm + Smooth Animations Ready! Respawn twice.",
Duration = 8,
Image = 4483362458
})
print("Rost Alpha ApexHub v2026 | Magic Bullets + Auto Farm Ready!")
